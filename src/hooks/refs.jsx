import { createRef } from "react";

export const tasksContainer = createRef(null);
export const taskID = createRef(null);
export const taskDate = createRef(null);
export const taskContent = createRef(null);
export const createTaskButton = createRef(null);
export const deleteTaskButton = createRef(null);
export const deleteAllTaksButton = createRef(null);